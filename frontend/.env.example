# Backend API Endpoint Base URL
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# CDN URL for static assets (images, videos, logo)
# If empty, the app falls back to serving assets from local /public
NEXT_PUBLIC_ASSET_CDN_URL=
